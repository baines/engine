TODO

cvars need defaults
trie for cvar names
cvar funcs
	default func for cvars that sets / gets them
cli stuff
	pretty print cvar names / types / default / current_val / min / max e.t.c.
	tab complete
	fancy bg texture / shader
blend modes + primative type in renderable
shader uniforms
engine logging
