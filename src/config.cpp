#include "config.h"

void Config::load(int argc, char** argv){
/* TODO: 
	+ parse arguments with "-", like -w for windowed, -c for config file location
	+ parse arguments with "+" by calling cvar funcs
*/

}
